services:
  cve-feed:
    build: .
    container_name: cve-feed-explorer
    ports:
      - "8082:80"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: "2.0"
